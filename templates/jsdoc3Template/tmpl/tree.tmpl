var tree = [
	["Leap JavaScript API Reference", "class_index.html", "", [
		<?js 
			docs.forEach(function(doc, i) {
				print(render('tree_leaf.tmpl', doc));
				if(i < docs.length - 1) {
					print(',');
				}
			}); 
		?>
	]]
];
